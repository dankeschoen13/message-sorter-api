# New Project

Under development. This is a RESTful API with an endpoint that accepts JSON payload of a customer message, processes it through an AI model, and stores the structured result in a PostgreSQL database.